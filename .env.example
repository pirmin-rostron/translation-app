# Translation provider: "openai" to use OpenAI, leave empty for mock
TRANSLATION_PROVIDER=openai

# Required when TRANSLATION_PROVIDER=openai
OPENAI_API_KEY=

# Segments per API call (default 5)
TRANSLATION_BATCH_SIZE=5
